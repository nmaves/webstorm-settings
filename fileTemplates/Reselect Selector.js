import { createSelector } from 'reselect'

const slice = state => state.slice
const slice2 = state => state.slice2

const ${NAME} = (slice, slice2) => {
	
}

export const ${NAME} = createSelector(
	${NAME}
)